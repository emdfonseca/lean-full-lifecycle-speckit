# lifecycle-greenfield-bootstrap

Bootstraps the reusable framework first and, optionally, product intent and a first Ready queue.

Run:

```bash
specify workflow run lifecycle-greenfield-bootstrap [inputs]
```
